package operations;

import object.Matrix;
import operations.interfaces.Computation;

public class Multiplication implements Computation {
    @Override
    public double[][] compute(Matrix matrix1, Matrix matrix2) {
        return new double[0][];
    }
}
